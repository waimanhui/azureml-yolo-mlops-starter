$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command
display_name: yolo-train
experiment_name: yolo-retraining
compute: azureml:yolo-gpu-cluster
code: ../..
environment:
  image: ultralytics/ultralytics@sha256:5d797311857ea3d98c24ed0f7f78d27902b72ea5fdec93307f63634988abd131
inputs:
  training_data:
    type: uri_folder
    path: azureml:yolo-training-data:1
    mode: download
  base_model: yolo11n.pt
  epochs: 50
  image_size: 640
  batch_size: 16
  device: "0"
outputs:
  model_output:
    type: uri_folder
    mode: upload
command: >-
  python src/train.py
  --data ${{inputs.training_data}}/data.yaml
  --model ${{inputs.base_model}}
  --epochs ${{inputs.epochs}}
  --image-size ${{inputs.image_size}}
  --batch-size ${{inputs.batch_size}}
  --device ${{inputs.device}}
  --model-output ${{outputs.model_output}}
limits:
  timeout: 21600